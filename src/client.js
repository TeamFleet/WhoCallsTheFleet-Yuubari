import superClient from '../system/super3/client'
import {
    i18n,
    router,
    redux,
    client,
} from '../super'

export default superClient({
    i18n,
    router,
    redux,
    client,
})
