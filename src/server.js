import superServer from '../system/super3/server'
import {
    name,
    dir,
    dist,
    template,
    i18n,
    locales,
    router,
    redux,
    client,
    server,
} from '../super'

superServer({
    name,
    dir,
    dist,
    template,
    i18n,
    locales,
    router,
    redux,
    client,
    server,
})
